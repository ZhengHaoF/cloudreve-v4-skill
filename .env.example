# Cloudreve v4 实例地址（不含 /api/v4 后缀）
CLOUDREVE_URL=https://your-cloudreve.example.com

# 方式一：直接提供 API Token（推荐，优先于账号密码）
CLOUDREVE_TOKEN=

# 方式二：账号密码登录（与 Token 二选一；不支持 2FA）
CLOUDREVE_EMAIL=
CLOUDREVE_PASSWORD=
